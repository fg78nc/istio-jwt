apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: istio-jwt-demo
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: backend
